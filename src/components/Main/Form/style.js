import { makeStyles } from "@material-ui/core";

export default makeStyles(()=>({
    radioGroup: {
        display: 'flex',
        justifyContent: 'centre',
        marginBottom: '-10px',
    },
    button: {
        marginTop: '20px',
    },
}))
